# P3-QA-01 - Quality and Defect List/Create/Detail

## Scope

Verify Quality dashboard fields, Defect State rules, Work Item Flow/Schedule fields and Defect creation.

## Recorded DevInt execution

- Quality dashboard displayed Rank, ID, Name, User Story, Severity, Priority, State, Flow State, Fixed In Build, Iteration, Submitted By and Owner.
- Defect State transitions matched the SRS state-machine constraints.
- Flow State exposed the six Work Item values and remained separate from Defect State.
- Severity and Priority option sets matched the baseline.
- Create Defect was submitted twice with Name, Severity and Priority.
- Each submission closed the modal without a visible error, but created no row and left Open Defects at `3`.

## Result and gap

Result: **Partial** because list/detail behavior matched but the primary create function failed.

`GAP-P3-QA-001` is a P0 functional gap. Dev must fix Defect creation from `Quality > Defect` and return actionable validation when creation fails.

## Test-data and evidence state

The failed submissions created no retained Defect. No durable screenshot was committed; capture request/result evidence during retest.
