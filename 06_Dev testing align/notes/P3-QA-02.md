# P3-QA-02 - Defect Cross-Screen Synchronization

## Scope

Verify two-way Schedule/Flow synchronization across Quality and Work Item Detail while Defect State remains independent.

## Recorded DevInt execution

- Used `DE-2`.
- Changed Flow State from `Defined` to `In-Progress` on Quality.
- Detail Schedule State became `In-Progress`.
- Changed Detail Schedule State from `In-Progress` to `Completed`.
- Quality Flow State became `Completed`.
- Defect State remained `Submitted`.
- Both screens remained consistent after Save.
- Mirror recalculated on Save rather than in the unsaved preview.

## Result

Result: **Pass / Match**.

This is a positive delta from the earlier E2E finding that reported missing mirror behavior and incomplete Flow State options.

## Test-data and evidence state

`DE-2` was restored to Schedule/Flow `Defined` and Defect State `Submitted`. No durable screenshot was committed; capture the before/after values during regression.
